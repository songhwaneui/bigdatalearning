자바스크립트 메소드 JSON.stringify, JSON.parse()

파이썬 메소드, JSON.dump(), JSON.load()

프론트 -> 서버 (stringify(), dump())

서버 -> 프론트 (parse(), loads())
=======================================

json 언어종속x
json은 싹다 <script></script>에 쓴다

네트워크 넘길떄는 무조건(byte 타입) -> serialize 



console.log(obj);   
json 포맷
출력하면 { a:~~, b: ~~} 이꼴 딕셔너리와 비슷
키뽑을수있음

console.log("============================")
console.log(JSON.stringify(obj));
json포맷을 스트링화한것
출력하면 { a:~~, b: ~~} 이꼴이지만 문자형이라 키뽑고 이지랄 못함



re os 직렬화, json 직렬화 v=

re!! 직렬화, 파셀러블



================================================================================
HTTP
지능형 = 머신러닝

서버에서 HTTP
브라우저에서는 HTTP받고 그걸가지고 렌더링
브라우저마다 HTTP해석하는게 다름(표준이 다다름) 3d 프론트 explore ,edge때문에 뒤짐

서버에서는 로드밸런싱, 서버구축, 대용량시스템

cf) 분산 병렬 처리 = 논리적으로 떠잇는 서버 데몬, 아
http통신하면 무조건 html로 돌려받음 우리가 받아볼자료 따라서 html 파싱할줄 알아야됨

자료 파싱해서 => 머신러닝인풋데이터로씀


url은 dns보다 좀더 길다.
서버단에서 
어떤파일을 요청할건지 서




==============================

request
url에 ?~에 붙여서 
accept  text/html -> text를 html로, */* 이미지나 동영상 다받아

user-agent 서버쪽에서는 어떤사람인지 걸러야됨 인증 느낌 (어떤 os , 어떤브라우져 등등~)


get, post =http method
post 길이 제한 x 여러형태 컨텐츠 넘길수 있따.
get 길이 제한 o, 텍스트만 넘길수 있다.

어떤사이트 url 안보이게, 잘바뀌는것 왓다갓다
안보이는건 아이프레임 그거다 뚫어야함


get                      //////== select 같은거 read

post 회원가입같은거 할떄  validation검사토와할때                     ///////// == insert같은기능 create


crud  rest rest방식 api 
서버의 local 경로 알수없음
이떄는 어디로분기하고 이떄는 어디로분기하고 이런거 알수있다
요세는 다 rest full방식으로 설계 


cf) 공공데이터 포탈에 get만 되는데 post put방식으로 메소드 데이터 늘려고 하면 전화옴

resp response 객체


response headers 에서 올때
content-type : text/html; charset=utf-8 아니면 아스키 euc 이러면 인터넷 한글이 깨져서 나올수도 있다.
status: 200 너의 응답에 잘 응답했다. 404 에러 

json이면 application/json

이런거 다 헤더에 붙어있다.

body부분에는 html 코드가 다 들어있다. 브라우저에서 ui대로 다 밀어넣음, 클릭했을때 드랍다운, 이런 엑티비티 동작들은 자바스크립트=> 아우러져서 인터넷브라우져(ex 네이버)

===================================================================
데이터 맞게끔 모델 피팅 -> 모델돌림

bot, 크롤러 ,스파이더, agent => 니가알아서 수집해와라 해서 데이터 수집

서버에서 가져온 데이터는 의미없음
db 에서 가져와서 해석한 html  끊임없이 스크래핑하면 결국 db를 다 가져올수 있다
db다가져오면 저작권 걸림

새로운 창업하고싶다. ->대기업껀안되고 
 중소기업 서비스 싹다 카피 뜨면 고대로 따라할수있음 고소고발해도 변호사잘씀

화이트리스트 opt in ~~수집을 허용하도록 체크,

어디어디 가져가지마  블랙리스트 opt out
니가 명시적으로 거부안했으니 내가하고싶은거 할거다

opt - in 반드시 동의해야한 할수있는거

cf)
si업체 => 다단계 공식적인합법화

필리핀,일본,인도
1억 // 2천만원 주고 서버비천만원 나머지 먹고
다 중간관리자임. 진짜 관리자는 숨어있음
소라넷 개발운영자 (한달에 3백만원주고관리해준거밖에없다. ) 해외등록 ip -> 관리필요 못잡다가 이제잡음
